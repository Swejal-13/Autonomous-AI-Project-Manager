{
    "name": "nexo-root",
    "version": "1.0.0",
    "description": "Root project to run full stack",
    "scripts": {
        "dev": "docker-compose up -d && npx concurrently \"npm run dev --prefix frontend\" \"cd backend && venv\\Scripts\\python -m uvicorn app.main:app --reload\"",
        "setup:win": "npm install && npm install --prefix frontend && cd backend && venv\\Scripts\\pip install -r requirements.txt"
    },
    "devDependencies": {
        "concurrently": "^8.2.0"
    }
}